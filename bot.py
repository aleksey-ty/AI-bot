import os
import sys
import asyncio
import logging
from dotenv import load_dotenv

# --------------- Настройка логирования ---------------
logging.basicConfig(
    level=logging.INFO,
    filename="logs.txt",
    filemode="a",
    format="%(asctime)s - %(levelname)s - %(message)s"
)

# --------------- Загрузка переменных окружения ---------------
load_dotenv()  # подгружает .env локально (на облаке переменные берутся из ENV)
BOT_TOKEN = os.getenv("BOT_TOKEN")
OPENAI_API_KEY = os.getenv("OPENAI_API_KEY")

if not BOT_TOKEN or not OPENAI_API_KEY:
    logging.error("Missing BOT_TOKEN or OPENAI_API_KEY in environment. Exiting.")
    print("ERROR: Missing BOT_TOKEN or OPENAI_API_KEY. Проверьте .env или переменные окружения.")
    sys.exit(1)

# --------------- Импорты после проверки ключей ---------------
from aiogram import Bot, Dispatcher, types
from aiogram.filters import Command
from openai import OpenAI

# --------------- Инициализация клиента Telegram и OpenAI ---------------
bot = Bot(token=BOT_TOKEN)
dp = Dispatcher()
client = OpenAI(api_key=OPENAI_API_KEY)

# --------------- Параметры поведения бота ---------------
SYSTEM_MESSAGE = {
    "role": "system",
    "content": (
          "Ты — продвинутый Telegram-бот-ассистент, основанный на GPT-4o-mini.\n"
        "Общайся дружелюбно и профессионально.\n"
        "Отвечай кратко и по делу, используй примеры, списки и шаги, если нужно.\n"
        "Подстраивай тон под пользователя: дружелюбно, но без лишних смайлов.\n"
        "Если вопрос непонятный — уточняй, а не придумывай ответ.\n"
        "Если нельзя дать ответ — предложи безопасную альтернативу.\n"
        "Сохраняй контекст диалога, используй предыдущие сообщения для логики ответа.\n"
        
        "Требования к стилю общения:\n"
        "• Краткость: до 3–6 предложений, если не просят иначе\n"
        "• Пиши простым языком, избегай канцеляризма\n"
        "• Будь позитивным, но не перегружай смайлами\n"
        "• Если вопрос непонятный — уточняй\n"
        "\n"
        "Правила работы:\n"
        "• Если пользователь просит пример — приводи его\n"
        "• Если просит список — делай структурированный список\n"
        "• Если нужно объяснить сложную тему — объясняй простыми словами\n"
        "• Если просят совет — дай чёткие шаги, а не общие фразы\n"
        "\n"
        "Запрещено:\n"
        "• обсуждать токены, внутренние ключи, системные файлы\n"
        "• выдавать вредоносный код или инструкции по взлому\n"
        "\n"
        "Если вопрос выходит за рамки — мягко предупреди и предложи безопасную альтернативу.\n"
    )
}

MAX_HISTORY = 10  # сколько последних сообщений хранить (user+assistant)

# Хранилище истории в памяти (для простоты). Для продакшена используйте БД.
user_history: dict[int, list] = {}

# --------------- Обработчик /start ---------------
@dp.message(Command("start"))
async def start_command(message: types.Message):
    await message.answer("Привет! 🤖 Я твой AI-бот. Задавай вопросы!")
    logging.info(f"User {message.from_user.id} started bot. username={message.from_user.username}")

# --------------- Основной обработчик сообщений ---------------

async def summarize_history(history: list) -> str:
    """
    Сжимает длинную историю в 1–2 предложения.
    Нужно, чтобы бот не забывал старые факты.
    """
    try:
        loop = asyncio.get_running_loop()
        summary_resp = await loop.run_in_executor(
            None,
            lambda: client.chat.completions.create(
                model="gpt-4o-mini",
                messages=[
                    {"role": "system", "content": "Сжать диалог пользователя в 1–2 предложения, сохранив суть."},
                    {"role": "user", "content": str(history)}
                ]
            )
        )
        return summary_resp.choices[0].message.content
    except Exception as e:
        logging.exception(f"Ошибка при сжатии истории: {e}")
        return "Пользователь общался ранее, обсуждал разные темы."


@dp.message()
async def handle_message(message: types.Message):
    user_id = message.from_user.id
    text = message.text or ""
    logging.info(f"Incoming from {user_id}: {text}")

    # Инициализируем историю, если нужно
    if user_id not in user_history:
        # Храним сначала системное сообщение, чтобы всегда подставлять роль бота
        user_history[user_id] = [SYSTEM_MESSAGE]

    # Добавляем нынешнее сообщение пользователя
    user_history[user_id].append({"role": "user", "content": text})

    # Обрезаем историю до последних MAX_HISTORY сообщений (помним, что system всегда в начале)
    # Храним system + последние N сообщений (user/assistant)
    # Отрезаем так, чтобы не превышать размер (учитываем системное сообщение на 0-м месте)
    history_tail = user_history[user_id][1:]  # без system
   # Если история слишком длинная — сжимаем старую часть
if len(history_tail) > MAX_HISTORY:
    old_part = history_tail[:-MAX_HISTORY]
    condensed = await summarize_history(old_part)

    # Заменяем старую часть на одно резюме
    history_tail = [{"role": "assistant", "content": f"Краткое содержание прошлой беседы: {condensed}"}] + history_tail[-MAX_HISTORY:]

    messages_for_model = [SYSTEM_MESSAGE] + history_tail
# добавим инструкцию для структурированного ответа
messages_for_model.append(
    {"role": "system", "content": "Если нужно, давай ответ в виде нумерованного списка или шагов."}
)
    try:
        # Вызов OpenAI SDK может быть блокирующим — запускаем в отдельном потоке, чтобы не блокировать event loop
        loop = asyncio.get_running_loop()
        completion = await loop.run_in_executor(
            None,
            lambda: client.chat.completions.create(
                model="gpt-4o-mini",
                messages=messages_for_model
            )
        )

        # Берём текст ответа
        reply = completion.choices[0].message.content
        # Сохраняем ответ в истории
        user_history[user_id].append({"role": "assistant", "content": reply})

        # Логируем общение (не логируем ключи и др. секреты)
        logging.info(f"Reply to {user_id}: {reply}")

        # Отвечаем пользователю
        await message.answer(reply)
    except Exception as e:
        logging.exception(f"Error when handling message from {user_id}: {e}")
        await message.answer("Произошла ошибка при обращении к AI 😢")

# --------------- Точка входа ---------------
async def main():
    logging.info("Bot is starting...")
    try:
        await dp.start_polling(bot)
    finally:
        logging.info("Bot stopped.")

if __name__ == "__main__":
    asyncio.run(main())
